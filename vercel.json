{
  "devCommand": "vite --port $PORT",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "crons": [
    { "path": "/api/cron/npc-tick", "schedule": "0 * * * *" }
  ],
  "routes": [
    { "src": "/api/(.*)",      "dest": "/api/$1"   },
    { "src": "/@(.*)",         "dest": "/@$1"       },
    { "src": "/__vite(.*)",    "dest": "/__vite$1"  },
    { "src": "/(.+\\.[^/]+)", "dest": "/$1"         },
    { "src": "/(.*)",          "dest": "/index.html" }
  ]
}
